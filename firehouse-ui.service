# Copy to /etc/systemd/system/firehouse-ui.service
# Edit User, WorkingDirectory, and ExecStart paths to match your setup
[Unit]
Description=Firehouse UI
After=network.target

[Service]
Type=simple
# CHANGE THESE to match your user and install path:
User=YOURUSER
WorkingDirectory=/home/YOURUSER/firehouse-ui
ExecStart=/home/YOURUSER/firehouse-ui/venv/bin/uvicorn app:app --port 8000 --host 0.0.0.0
Restart=always
RestartSec=3
EnvironmentFile=-/home/YOURUSER/firehouse-ui/.env

[Install]
WantedBy=multi-user.target
